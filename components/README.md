This folder is reserved for reusable LVGL XML components.

The current online preview project uses direct screen XML files, but this
placeholder is intentionally kept because LVGL Online Share checks for
non-XML files in standard project folders when detecting valid projects.
